# 🦈 Wireshark — полный курс анализа сетевого трафика (A–Z)

**Уровень:** Networking · Blue Team · SOC · Pentest · Cloud Security
**Формат:** теория + лаборатории + CTF + практический курс
**Автор:** UncleDeniy

---

## 📌 Что такое Wireshark и зачем он нужен

**Wireshark** — это профессиональный анализатор сетевого трафика (packet sniffer), позволяющий работать с сетевыми пакетами на самом низком уровне. Он показывает **реальное поведение сети**, а не то, что «думает» приложение или операционная система.

Wireshark позволяет:

* перехватывать трафик в реальном времени;
* анализировать каждый пакет побитово;
* изучать заголовки и поля протоколов;
* диагностировать сложные сетевые проблемы;
* расследовать инциденты информационной безопасности.

На практике Wireshark используют, когда:

* логи не дают ответа;
* мониторинг показывает симптом, но не причину;
* требуется forensic-анализ после инцидента;
* нужно доказать факт атаки или утечки.

Wireshark — инструмент уровня **SOC / Blue Team / Pentest**, а не просто «сниффер для студентов».

---

## 🧠 БАЗОВАЯ ТЕОРИЯ (обязательно перед практикой)

### 🔹 Как работает Wireshark

1. Wireshark подключается к сетевому интерфейсу
2. Захватывает **сырые пакеты**
3. Декодирует их по протоколам
4. Показывает:

   * список пакетов
   * детали каждого уровня
   * байты пакета

### 🔹 Уровни TCP/IP в Wireshark

| Уровень      | Примеры                     |
| ------------ | --------------------------- |
| Канальный    | Ethernet, ARP               |
| Сетевой      | IP, ICMP                    |
| Транспортный | TCP, UDP                    |
| Прикладной   | DNS, HTTP, HTTPS, FTP, SMTP |

---

## 🗺 Архитектура TCP/IP (инфографика)

![TCP/IP overview](https://docs.oracle.com/cd/E18752_01/html/816-4554/figures/ipov.fig88.png)

![TCP/IP stack](https://studfile.net/html/1334/253/html_sY87aRtwvX.FPG2/img-XKxnW4.png)

![Ports map](https://avatars.mds.yandex.net/i?id=07bcd38fb3d6e8038311092ffc8b43199fb03db25dd060de-11080322-images-thumbs&n=13)

---

# 🪟 РАЗДЕЛ 1. WIRESHARK НА WINDOWS

## 🔧 Установка

1. Скачать: [https://www.wireshark.org/download.html](https://www.wireshark.org/download.html)
2. Запустить установщик
3. **Обязательно отметить:**

   * ✅ Npcap (Packet Capture Library)
   * ❌ USBPcap (если не работаешь с USB)

> ⚠️ Без Npcap Wireshark **НЕ БУДЕТ** захватывать трафик.

---

## ▶️ Первый запуск и захват

1. Запусти Wireshark **от имени администратора**
2. Выбери интерфейс:

   * Ethernet — проводная сеть
   * Wi‑Fi — беспроводная
3. Нажми **Start**

---

## 🔍 Основные фильтры (Display Filter)

```text
ip.addr == 8.8.8.8
tcp.port == 443
dns
http.request
icmp
```

---

# 🍎 РАЗДЕЛ 2. WIRESHARK НА macOS

## 🔧 Установка

* [https://www.wireshark.org/download.html](https://www.wireshark.org/download.html)

или через Homebrew:

```bash
brew install --cask wireshark
```

## 🔐 Права доступа (ВАЖНО)

System Settings → Privacy & Security → разрешить packet capture

Иногда требуется:

```bash
sudo chmod +rw /dev/bpf*
```

---

# 🐧 РАЗДЕЛ 3. WIRESHARK НА LINUX

## 🔧 Установка

### Debian / Ubuntu

```bash
sudo apt install wireshark
```

### Arch Linux

```bash
sudo pacman -S wireshark-qt
```

## 🔐 Разрешения

```bash
sudo usermod -aG wireshark $USER
newgrp wireshark
```

---

# 🧨 9. PENTEST ONLY (УЧЕБНЫЙ РЕЖИМ)

> ⚠️ **ВАЖНО**
> Всё ниже — **ТОЛЬКО для учебной лаборатории**, своих VM, CTF, HackTheBox, TryHackMe.
> Применение в реальных сетях без разрешения — незаконно.

---

## 🎯 Цель Pentest-части

Научиться:

* видеть атаки **на уровне пакетов**;
* понимать, **как выглядят атаки в сети**;
* использовать Wireshark как **инструмент пентестера и аналитика**.

---

## 🧱 10. ЛАБОРАТОРНАЯ СРЕДА (VM)

```
[ Kali Linux ]  ← attacker
        |
[ Victim Linux / Windows ]
        |
[ Router / Gateway ]
```

---

# 🧪 11. ЛАБА №1 — ПЕРЕХВАТ HTTP (BASIC)

### Теория

HTTP — протокол прикладного уровня **без шифрования**. Любой, кто имеет доступ к каналу передачи данных, может:

* видеть URL и параметры запросов;
* читать cookies и заголовки;
* перехватывать логины и пароли.

### Практика

Фильтр:

```text
http
```

Анализируй:

* методы GET / POST;
* заголовки `Host`, `User-Agent`, `Cookie`;
* тело запросов.

### Вывод

Использование HTTP для аутентификации — критическая ошибка безопасности.

---

# 🧪 12. ЛАБА №2 — MITM (Man‑in‑the‑Middle)

Attacker:

```bash
sudo bettercap -iface eth0
```

В bettercap:

```text
arp.spoof on
net.sniff on
```

Wireshark:

```text
arp
arp.opcode == 2
```

Признаки:

* один MAC → много IP
* частые ARP reply без request

---

# 🧪 14. ЛАБА №4 — DNS SPOOFING

```text
dns
```

Признаки:

* ответ быстрее запроса
* несколько ответов
* IP не совпадает с реальным

---

# 🧪 15. PASSWORD SNIFFING

Уязвимые протоколы:

* FTP
* HTTP
* SMTP
* POP3
* Telnet

Фильтры:

```text
ftp
http.authorization
```

---

# 🧪 16. TLS ANALYSIS (ADVANCED)

```text
tls
```

Смотри:

* TLS version
* Cipher Suites
* ALPN
* Cert chain

Признаки проблем:

* TLS 1.0 / 1.1
* слабые cipher
* self‑signed cert

---

# 🧩 20. CTF НА WIRESHARK (PRACTICAL / BLUE TEAM)

CTF-блок имитирует реальные задачи SOC и Blue Team, где аналитик получает **PCAP-файл без контекста**.

Задача аналитика:

* определить, был ли инцидент;
* классифицировать тип атаки;
* найти источник и жертву;
* описать ход событий.

Именно в таком формате проверяют кандидатов на позиции SOC Analyst.

---

## 🧪 CTF №1 — Cleartext Credentials

```text
http.request.method == "POST"
```

Ищи:

* Authorization
* username=
* password=

---

## 🧪 CTF №2 — ARP Spoofing

```text
arp
arp.opcode == 2
```

---

## 🧪 CTF №3 — DNS Spoofing

```text
dns
```

---

## 🧪 CTF №4 — Suspicious HTTPS

```text
tls
```

---

# 🛡 21. BLUE TEAM КУРС (WIRESHARK)

Wireshark — **не IDS**, а **микроскоп**.

Используется для:

* расследования инцидентов;
* подтверждения алертов;
* анализа PCAP;
* forensic после атак.

---

## 🚨 Типовые инциденты

### Port Scan

```text
tcp.flags.syn == 1 && tcp.flags.ack == 0
```

### Brute Force

```text
ftp || ssh
```

### Beaconing (C2)

```text
ip.addr == X
```

---

# ☁️ 22. WIRESHARK + KUBERNETES SECURITY

Где ловить трафик:

* eth0
* cni0
* flannel.1
* calico

Pod:

```bash
kubectl exec -it pod -- tcpdump -i eth0
```

Фильтры:

```text
tcp.port == 6443
tcp.port == 10250
dns
```

---

# 🧠 23. WIRESHARK В SOC (WORKFLOW)

1. SIEM дал alert
2. Выгрузили PCAP
3. Wireshark:

   * filters
   * statistics
   * streams
4. Подтвердили / опровергли
5. Отчет

---

# 📦 24. ИТОГ ВСЕГО КУРСА

Этот курс формирует **глубокое и практическое понимание сети на уровне пакетов**.

После его прохождения ты:

* уверенно читаешь PCAP-файлы;
* отличаешь нормальный трафик от атак;
* понимаешь MITM, spoofing, scans;
* готов к ролям Junior SOC Analyst, Blue Team, Pentester (lab).

Это фундамент для дальнейшего развития в:

* SIEM и Threat Hunting;
* Cloud Security;
* Advanced Blue Team.

---

# 🔥 25. ГЛАВЫ 12–16 — ГЛУБОКИЙ АНАЛИЗ АТАК (STEP-BY-STEP)

Этот раздел объясняет **как атака выглядит в реальности**, **что именно видит аналитик** и **где чаще всего ошибаются новички**.

---

## 🔥 25.1 MITM / ARP SPOOFING — КАК ВЫГЛЯДИТ АТАКА

### 🧠 Что происходит в сети

1. Атакующий и жертва находятся в одной L2-сети
2. Атакующий рассылает поддельные ARP Reply
3. Жертва думает, что атакующий — это gateway
4. Весь трафик идёт через атакующего

### 🔍 Что видит аналитик в Wireshark

Фильтры:
```text
arp
arp.opcode == 2
````

Признаки:

* один MAC-адрес «представляется» разными IP
* ARP Reply идут без ARP Request
* ARP-кэш обновляется слишком часто

### ❌ Типичные ошибки новичков

* смотреть только HTTP/HTTPS
* не проверять MAC-адрес шлюза
* думать, что HTTPS «прячет» MITM

📌 **Важно:**
HTTPS шифрует контент, но **MITM виден на уровне ARP всегда**.

---

## 🔥 25.2 DNS SPOOFING — ПОШАГОВО

### 🧠 Как работает атака

1. Клиент отправляет DNS-запрос
2. Атакующий отвечает быстрее реального DNS
3. Клиент принимает поддельный IP
4. Трафик уходит на фейковый сервер

### 🔍 Что видно в Wireshark

```text
dns
```

Признаки:

* несколько ответов на один запрос
* подозрительно маленькое время ответа
* IP не совпадает с реальным

### ❌ Ошибки новичков

* проверять только HTTP
* не сравнивать TTL
* игнорировать кеш DNS

---

## 🔥 25.3 TLS ANALYSIS — ЧТО МОЖНО УВИДЕТЬ БЕЗ РАСШИФРОВКИ

Даже без TLS-ключей Wireshark показывает:

* версию TLS
* Cipher Suites
* SNI (имя сервера)
* ALPN (http/1.1, h2)
* цепочку сертификатов

Фильтр:

```text
tls
```

### 🚩 Признаки проблем

* TLS 1.0 / 1.1
* self-signed сертификат
* слабые cipher suites
* отсутствие SNI

### ❌ Ошибки новичков

* пытаться читать HTTPS payload
* считать TLS «чёрным ящиком»
* игнорировать handshake

---

# 🛡 26. BLUE TEAM — MINI-КУРС SOC L1–L2

Этот раздел — **реальный учебник SOC**, а не теория.

---

## 🧠 26.1 Реальный workflow SOC

1. SIEM / IDS дал alert
2. Аналитик получил PCAP
3. Анализ в Wireshark
4. Классификация инцидента
5. Решение + отчёт

---

## 👶 SOC L1 — ЧТО ОН ДЕЛАЕТ

SOC L1 отвечает на вопросы:

* был ли инцидент?
* кто источник?
* кто жертва?
* когда началось?
* какой протокол?

Wireshark:

```text
dns
http
tcp
```

---

## 🧠 SOC L2 — ЧТО ДОБАВЛЯЕТ

SOC L2:

* подтверждает атаку
* ищет lateral movement
* оценивает масштаб
* пишет рекомендации

Фильтры:

```text
tcp.stream
ip.addr == X
```

---

## 📝 Как выглядит хороший отчёт

* Краткое описание
* Таймлайн
* Технические признаки
* Рекомендации

---

# ☁️ 27. KUBERNETES SECURITY — ПРОДВИНУТЫЙ УРОВЕНЬ

Это **редкий и ценный навык**, который реально отличает специалиста.

---

## 🔄 27.1 East–West трафик

В Kubernetes большинство атак происходит **внутри кластера**.

Фильтр:

```text
ip.addr == 10.244.x.x
```

Ищи:

* pod → pod соединения
* неожиданные направления
* постоянный фоновый трафик

---

## 🔐 27.2 mTLS и Service Mesh

Фильтр:

```text
tls
```

Проверяй:

* используется ли mTLS
* как часто меняются сертификаты
* ALPN (Envoy)

---

## 🚨 27.3 API Server Abuse

```text
tcp.port == 6443
```

Признаки:

* нестандартные клиенты
* частые запросы
* странные User-Agent

---

## 🕷 27.4 Lateral Movement в кластере

```text
tcp.port == 10250
```

Признак:

* доступ к kubelet
* перемещение между pod’ами

---

# 🧠 28. CTF — УРОВЕНЬ СОБЕСЕДОВАНИЯ

## ❓ Типовые вопросы

1. Был ли инцидент?
2. Какой тип атаки?
3. На каком уровне OSI?
4. Какие признаки?
5. Как защититься?

### ✅ Пример ответа

* Тип: ARP Spoofing
* Уровень: L2
* Признаки: ARP Reply без Request
* Митигирование: DHCP Snooping, ARP Inspection

---

# 📊 29. STATISTICS / IO GRAPHS — КЛЮЧЕВОЙ НАВЫК

## 🔥 Почему это важно

Statistics — **сердце анализа**, а не фильтры.

Меню:

* Protocol Hierarchy
* Conversations
* Endpoints
* IO Graphs

---

## 📈 Типовые паттерны

* резкие spikes → атака или баг
* ровные интервалы → C2 / beaconing
* рост DNS → tunneling

---


