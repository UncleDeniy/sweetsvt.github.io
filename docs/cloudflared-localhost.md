
# Делись своим localhost с кем угодно за секунды (Cloudflare Tunnel / cloudflared)

**Автор:** UncleDeniy  
**Категория:** DevOps → Networking Tools  
**Дата публикации:** 2026-01-04

---

## Введение

Когда ты разрабатываешь сайт/бота/API локально, почти всегда наступает момент, когда нужно:

- показать результат другу / заказчику;
- протестировать webhook (Telegram, Stripe, GitHub);
- открыть доступ с телефона, планшета или другого ПК;
- дать коллеге потыкать интерфейс без деплоя.

Обычно это превращается в боль: проброс портов, NAT, роутер, белый IP, VPN, временный сервер.

Решение проще: **Cloudflare Tunnel (cloudflared)** — позволяет публиковать локальный сервер в интернет за секунды **с HTTPS**.

---

## Что такое Cloudflare Tunnel

**Cloudflare Tunnel** создаёт защищённое соединение **изнутри наружу**:

- твой компьютер сам устанавливает исходящее соединение к Cloudflare;
- Cloudflare выдаёт публичный URL, который проксирует запросы на твой локальный сервис;
- никаких открытых портов на роутере;
- HTTPS включён автоматически.

✅ Это прямой конкурент ngrok, но часто удобнее для “быстро показать” и без лишней возни.

---

## Как это выглядит (схема)

![Cloudflare Tunnel diagram](https://developers.cloudflare.com/_astro/handshake.eh3a-Ml1_1IcAgC.webp)

![Zero Trust / Tunnel architecture](https://avatars.mds.yandex.net/i?id=a55cc51128a300049cf955dc38c29c9296e69987-4628623-images-thumbs&n=13)

---

## Установка cloudflared

### macOS (Homebrew)

```bash
brew install cloudflared
````

### Windows

Скачать и установить (официальная инструкция):
[https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/)

### Linux (Debian/Ubuntu)

```bash
wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
sudo dpkg -i cloudflared-linux-amd64.deb
```

---

## Быстрый запуск: поделиться localhost по HTTPS

Допустим, у тебя проект работает на `http://localhost:3000`.

```bash
cloudflared tunnel --url http://localhost:3000
```

В терминале появится публичная ссылка вида:

* `https://<случайный-домен>.trycloudflare.com`

Её можно дать кому угодно — и человек увидит твоё приложение.

---

## Примеры под разные порты

### Frontend (Vite/React/Next dev)

```bash
cloudflared tunnel --url http://localhost:5173
```

### Backend (API)

```bash
cloudflared tunnel --url http://localhost:8080
```

### Локальный сайт (nginx)

```bash
cloudflared tunnel --url http://localhost:80
```

---

## Почему это безопаснее “проброса портов”

Когда ты “открываешь порт” на роутере — ты по сути выставляешь сервис наружу.

Cloudflare Tunnel работает иначе:

✅ Нет входящих соединений напрямую на твой ПК
✅ Нет необходимости открывать порты
✅ HTTPS включён автоматически
✅ Соединение инициируется твоей машиной (outbound)
✅ Можно быстро выключить, закрыв терминал

---

## Cloudflared vs ngrok (по-людски)

| Функция                      | cloudflared                  | ngrok             |
| ---------------------------- | ---------------------------- | ----------------- |
| Быстрый старт без аккаунта   | ✅                            | ⚠️ часто требует  |
| HTTPS “из коробки”           | ✅                            | ✅                 |
| Проброс портов на роутере    | ❌                            | ❌                 |
| Стабильные домены            | ⚠️ (нужен аккаунт/настройка) | ✅ (обычно платно) |
| Лучшее для “быстро показать” | ✅🔥                          | ✅                 |

---

## Практические сценарии

### 1) Тест webhook (например Telegram)

* запускаешь локальный backend
* поднимаешь tunnel
* вставляешь HTTPS URL в webhook настройки

### 2) Демка проекта с телефона

* запускаешь фронтенд локально
* tunnel на `localhost:5173`
* открываешь ссылку на телефоне

### 3) Показ заказчику “прямо сейчас”

* без деплоя, без сервера
* просто ссылка

---

## Частые проблемы и решения

### “У меня не открывается ссылка”

* проверь, что локальный сервер реально запущен
* проверь правильный порт (`localhost:3000` vs `5173`)
* попробуй другой порт/проект

### “Нужно дать доступ только друзьям”

* самый быстрый вариант: даёшь ссылку только нужным людям
* для настоящего контроля доступа лучше Zero Trust (уже с аккаунтом Cloudflare)

---

## Мини-итог

Cloudflare Tunnel — один из самых быстрых способов расшарить localhost:

* **секунды до результата**
* **HTTPS автоматически**
* **без проброса портов**
* **идеально для демонстраций и тестов**

---

## Полезные ссылки

* Документация cloudflared: [https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/)
* Репозиторий cloudflared: [https://github.com/cloudflare/cloudflared](https://github.com/cloudflare/cloudflared)

